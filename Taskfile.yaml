version: '3'

tasks:
  default:
    cmds:
      - nodemon -q -e 'go' --signal SIGTERM --exec 'task build'

  build:
    cmds:
      - go build -o bin/inkube .
  run:
    cmds:
      - task: build
      - bin/inkube
